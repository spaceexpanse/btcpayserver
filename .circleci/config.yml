version: 2
jobs:
  build:
    machine: true
    steps: 
      - run: docker build .
  test:
    machine: true
    steps:
      - run: cd BTCPayServer.Tests 
      - run: docker-compose up tests


workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
